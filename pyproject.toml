[build-system]
requires = ["uv_build>=0.9.0,<0.10.0"]
build-backend = "uv_build"

[project]
name = "fastmcp-experiments"
version = "0.2.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp",
    "jsonref>=1.1.0",
]

# Development tool configuration
[tool.ruff]
target-version = "py313"
line-length = 88

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "UP",  # pyupgrade
]
ignore = [
    "E501", # line too long (handled by formatter)
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[dependency-groups]
dev = [
    "pytest-asyncio>=1.0.0",
    "pytest>=8.3.5",
    "ruff>=0.12.4",
]

[tool.pytest.ini_options]
asyncio_default_fixture_loop_scope = "function"  # fix deprecation warning

[tool.uv.sources]
# fastmcp = { path = "../fastmcp", editable = true }

# middleware-initialization is currently on 2.11.3
fastmcp = { git = "git+file:///Users/rmichael/Documents/Personal/Source/fastmcp/fastmcp", branch = "middleware-initialization" }
# fastmcp = { git = "git+file:///Users/rmichael/Documents/Personal/Source/fastmcp/fastmcp", branch = "modified-1641" }
